export default class AiClient {
  async sendCommandContext(commandPayload) {
    // Placeholder for future backend integration.
    // This stub exists to preserve clean API layer separation.
    return { success: true, data: null };
  }
}
